# FSInteractiveSettings.txt.tr.xlf

Pipeline role: XLIFF satellite localization resource for the FSInteractiveSettings string table (src/FSharp.Compiler.Interactive.Settings/FSInteractiveSettings.txt, base of FSInteractiveSettings.resx) of the FSharp.Compiler.Interactive.Settings assembly; the base table is empty, so the file carries an empty <body> and produces no satellite strings for the resource manager.

- File format: XLIFF 1.2 XML (xliff-core-1.2-transitional) with one <file> element, source-language "en", target-language "tr", original "../FSInteractiveSettings.resx".
- Localizes: FSInteractiveSettings table in src/FSharp.Compiler.Interactive.Settings/FSInteractiveSettings.txt (header comment only; no string entries).
- Locale: tr = Turkish.
- Trans-units: 0 (empty <body />).
- Conventions: placeholder file kept in sync with the base; no content to translate yet.