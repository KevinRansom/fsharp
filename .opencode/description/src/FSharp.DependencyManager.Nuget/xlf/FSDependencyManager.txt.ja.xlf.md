# FSDependencyManager.txt.ja.xlf

Pipeline role: XLIFF satellite localization resource for the FSDependencyManager string table (src/FSharp.DependencyManager.Nuget/FSDependencyManager.txt, base of FSDependencyManager.resx) used by the NuGet dependency manager that powers the `#r "nuget:"` feature; compiled into a satellite resource loaded by the resource manager for the Japanese culture.

- File format: XLIFF 1.2 XML (xliff-core-1.2-transitional) with one <file> element, source-language "en", target-language "ja", original "../FSDependencyManager.resx".
- Localizes: FSDependencyManager table in src/FSharp.DependencyManager.Nuget/FSDependencyManager.txt (package loading, source-directory and timeout/argument validation messages).
- Locale: ja = Japanese.
- Trans-units: 12.
- Conventions: indexed placeholders {0}/{1}; several units still status "new" (verbatim English copies); each unit carries an empty <note />.