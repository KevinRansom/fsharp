# FSDependencyManager.txt.en.xlf

Pipeline role: XLIFF satellite localization resource for the FSDependencyManager string table (src/FSharp.DependencyManager.Nuget/FSDependencyManager.txt, base of FSDependencyManager.resx) used by the NuGet dependency manager that powers the `#r "nuget:"` feature; the English file is a stub satellite resource for the invariant/English culture.

- File format: XLIFF 1.2 XML (xliff-core-1.2-transitional) with one <file> element, source-language "en", target-language "en", original "../FSDependencyManager.resx".
- Localizes: FSDependencyManager table in src/FSharp.DependencyManager.Nuget/FSDependencyManager.txt.
- Locale: en = English.
- Trans-units: 1 (only notUsed); the 11 functional strings live in the non-English satellite files.
- Conventions: state="new" single unit; keeps the resx source untranslated.