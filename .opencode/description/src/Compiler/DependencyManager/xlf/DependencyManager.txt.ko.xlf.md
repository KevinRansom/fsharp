# DependencyManager.txt.ko.xlf

Pipeline role: XLIFF satellite localization resource for the DependencyManager compiler string table (referenced as ../DependencyManager.resx) used by the package-manager and `#r` dependency-resolution logic in Compiler/DependencyManager; compiled into a satellite resource loaded by the resource manager for the Korean culture.

- File format: XLIFF 1.2 XML (xliff-core-1.2-transitional) with one <file> element, source-language "en", target-language "ko", original "../DependencyManager.resx".
- Localizes: the DependencyManager string table (no base .txt is checked in beside the xlf folder under src/Compiler/DependencyManager).
- Locale: ko = Korean.
- Trans-units: 3 (couldNotLoadDependencyManagerExtension, packageManagerError, packageManagerUnknown).
- Conventions: indexed placeholders ({0}, {1}, {2}) reused verbatim in translated targets; each unit carries an empty <note />.